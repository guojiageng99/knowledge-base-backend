<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.knowledge.base.file.mapper.FileMapper">
    <resultMap id="BaseResultMap" type="com.knowledge.base.file.entity.FileInfo">
        <id column="id" property="id"/>
        <result column="original_name" property="originalName"/>
        <result column="stored_name" property="storedName"/>
        <result column="file_path" property="filePath"/>
        <result column="file_size" property="fileSize"/>
        <result column="file_type" property="fileType"/>
        <result column="mime_type" property="mimeType"/>
        <result column="file_hash" property="fileHash"/>
        <result column="storage_type" property="storageType"/>
        <result column="bucket_name" property="bucketName"/>
        <result column="uploader_id" property="uploaderId"/>
        <result column="access_level" property="accessLevel"/>
        <result column="download_count" property="downloadCount"/>
        <result column="status" property="status"/>
    </resultMap>

    <sql id="BaseColumns">
        id, original_name, stored_name, file_path, file_size, file_type, mime_type, file_hash,
        storage_type, bucket_name, uploader_id, access_level, download_count, status,
        create_time, update_time, create_by, update_by, deleted
    </sql>

    <select id="selectByUploaderId" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/> FROM tb_file
        WHERE uploader_id = #{uploaderId} AND deleted = 0 ORDER BY create_time DESC
    </select>
    <select id="selectByFileType" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/> FROM tb_file
        WHERE file_type = #{fileType} AND status = 1 AND deleted = 0 ORDER BY create_time DESC
    </select>
    <select id="selectByFileHash" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/> FROM tb_file
        WHERE file_hash = #{fileHash} AND deleted = 0 LIMIT 1
    </select>
    <select id="selectByStorageType" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/> FROM tb_file
        WHERE storage_type = #{storageType} AND deleted = 0 ORDER BY create_time DESC
    </select>
    <select id="selectByAccessLevel" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/> FROM tb_file
        WHERE access_level &lt;= #{accessLevel} AND status = 1 AND deleted = 0 ORDER BY create_time DESC
    </select>
    <update id="incrementDownloadCount">
        UPDATE tb_file SET download_count = download_count + 1 WHERE id = #{fileId} AND deleted = 0
    </update>
    <update id="updateStatus">
        UPDATE tb_file SET status = #{status} WHERE id = #{fileId} AND deleted = 0
    </update>
    <select id="checkFileHashExists" resultType="boolean">
        SELECT COUNT(*) &gt; 0 FROM tb_file WHERE file_hash = #{fileHash} AND deleted = 0
    </select>
    <select id="countByUploaderId" resultType="long">
        SELECT COUNT(*) FROM tb_file WHERE uploader_id = #{uploaderId} AND deleted = 0
    </select>
    <select id="sumFileSizeByUploaderId" resultType="long">
        SELECT COALESCE(SUM(file_size), 0) FROM tb_file
        WHERE uploader_id = #{uploaderId} AND status = 1 AND deleted = 0
    </select>
    <select id="selectByBucketName" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/> FROM tb_file
        WHERE bucket_name = #{bucketName} AND deleted = 0 ORDER BY create_time DESC
    </select>
</mapper>
