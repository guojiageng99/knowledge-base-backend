# 企业知识库功能验收操作说明

本文按 `html/01` 到 `html/41` 的教程顺序整理。状态含义：

- **可验收**：当前代码和部署条件具备，可以按步骤看到预期结果。
- **部分可验收**：页面或接口存在，但有外部服务、配置或未覆盖的闭环。
- **暂不可验收**：当前环境缺少必要服务或真实配置。
- **无前端操作**：本节是架构、数据库或基础设施内容，需要通过日志、SQL 或接口验证。

## 统一准备

1. 打开知识库前端并登录。
2. 普通用户验收普通文档功能；管理员账号验收用户、审核、权限、系统设置和统计功能。
3. 每次点击后观察三类现象：页面是否跳转、按钮是否进入 loading、右下角是否出现成功或错误提示。
4. 看到“服务暂不可用”或“检查部署”的提示时，不要反复点击，应先启动对应后端服务和中间件。

## 01-04 基础架构与公共服务

### 01 企业智能知识库系统设计

- **功能点**：微服务拆分、网关、用户认证、文档、AI、搜索、文件、图谱和统计服务的架构设计。
- **前端操作**：无。通过服务清单、Docker Compose、网关路由和启动日志验收。
- **当前状态**：无前端操作。

### 02 数据库表设计

- **功能点**：用户、角色、权限、团队、文档、分类、标签、评论、审核、版本、搜索、文件、通知和统计表。
- **前端操作**：无。执行数据库初始化脚本后，用 `SHOW TABLES`、表结构和初始化数据验证。
- **当前状态**：无前端操作。

### 03 搭建后端项目骨架

- **功能点**：父 Maven 工程、公共模块、统一响应、基础实体、异常、自动填充和用户服务骨架。
- **前端操作**：无。执行 `mvn -DskipTests package`，确认各子模块打包成功。
- **当前状态**：无前端操作。

### 04 公共基础服务

- **功能点**：字典、系统配置、通知、操作日志、Redis、RabbitMQ、WebSocket 和 Knife4j。
- **前端操作**：本节没有独立的公共服务页面。后续可从“通知”进入通知中心、从“系统设置”进入配置页面。
- **接口验收顺序**：打开 Knife4j，依次调用字典查询/新增、系统配置查询/更新、通知分页/已读、操作日志分页接口。
- **预期现象**：接口返回统一 `Result`；新增或更新后再次查询能看到数据。
- **当前状态**：接口和后续页面存在；WebSocket、邮件等依赖需要单独验收。

## 05-11 文档基础能力

### 05 文档服务

- **功能点**：文档 CRUD、分类、标签、评论、版本、审核、MongoDB 内容存储。
- **前端操作**：进入“文档” -> 点击“新建文档” -> 填标题和 Markdown -> 选择“保存草稿”或“提交审核” -> 点击保存。
- **预期现象**：成功提示；返回文档列表；草稿出现在“草稿”；提交审核的文档出现在审核任务中。
- **当前状态**：可验收，前提是 MySQL、MongoDB、网关、文档服务运行。

### 06 分类

- **操作顺序**：进入左侧“分类” -> 点击“新建分类” -> 填名称、编码、描述、排序和状态 -> 点击“保存”。
- **子分类**：先点击左侧分类树中的父分类 -> 点击“新建子分类” -> 填表 -> 保存。
- **编辑/删除**：选中分类 -> 点击“编辑分类”或“删除” -> 确认弹窗。
- **预期现象**：分类树刷新，右侧显示分类详情和文档数。
- **当前状态**：可验收。

### 07 标签

- **功能点**：标签创建、修改、查询、删除以及文档标签关联。
- **前端操作**：进入左侧“标签” -> 点击“新建标签” -> 填名称、编码、分类 ID、类型、颜色和状态 -> 点击“保存”；列表中可搜索、编辑和删除。
- **当前状态**：标签管理页面已接入并可验收；文档编辑页的标签选择和文档标签关联仍需继续扩展。

### 08 版本

- **功能点**：文档版本记录、版本列表、版本详情和恢复指定版本。
- **前端操作**：进入“文档” -> 点击文档标题 -> 进入详情 -> 点击“编辑”修改并保存 -> 通过版本接口或详情页版本区域查看历史 -> 选择版本恢复并确认。
- **预期现象**：恢复后当前文档内容变为所选版本，且生成新的版本记录，不能直接覆盖历史版本。
- **当前状态**：可验收。文档详情页已提供“版本历史”，支持查看版本、选择两个版本对比和恢复指定正式版本。

### 09 评价、评论和点赞

- **操作顺序**：进入“文档” -> 打开文档详情 -> 点击“点赞” -> 点击“收藏” -> 在评论框输入内容 -> 点击“发表评论”。
- **评论点赞**：在评论列表中点击评论右侧点赞按钮。
- **预期现象**：按钮状态切换，计数增加；刷新页面后状态仍保持；评论出现在列表。
- **当前状态**：可验收，依赖文档服务和数据库。

### 10 文档审核

- **提交顺序**：新建/编辑文档 -> 选择“提交审核” -> 点击“提交审核”。
- **管理员顺序**：管理员进入“审核” -> 切换“待审核” -> 点击文档标题或“预览” -> 输入审核意见 -> 点击“通过”或“驳回”。
- **预期现象**：通过后文档状态变为已发布；驳回后状态变为已驳回并保存意见；批量通过会一次处理当前待审核任务。
- **当前状态**：可验收，需管理员账号和文档审核数据。

### 11 网关统一返回值

- **功能点**：统一响应、状态码、业务异常、Feign 拦截器、日志过滤器、CORS 和网关异常处理。
- **前端操作**：无专门页面。可在浏览器 Network 面板观察所有请求的统一响应结构；访问不存在接口观察错误提示而不是网关堆栈。
- **当前状态**：无前端操作，属于接口和网关验收。

## 12-18 认证、文档工作流与文件

### 12 登录和认证

- **登录顺序**：打开登录页 -> 输入用户名和密码 -> 点击“登录”。
- **预期现象**：成功后保存 Token 并跳转工作台；刷新页面仍保持登录；未登录直接访问 `/documents` 会跳回登录页。
- **退出顺序**：右上角账户菜单 -> 点击“退出登录” -> 回到登录页。
- **当前状态**：可验收。

### 13 文件上传

- **操作顺序**：进入“文档” -> 点击“导入”或进入“文件” -> 选择文件 -> 等待上传进度完成。
- **预期现象**：返回文件 ID/URL；文档导入页显示解析结果；头像上传后头像立即更新。
- **当前状态**：基础上传可验收，需 RustFS/file 服务运行；媒体转码另见 24。

### 14 文档查询、创建和删除

- **操作顺序**：进入“文档” -> 输入关键词 -> 点击“查询” -> 点击标题查看详情 -> 点击“编辑”修改 -> 返回列表 -> 点击删除并确认。
- **预期现象**：列表分页和筛选变化；详情显示 Markdown；删除后列表移除文档。
- **当前状态**：可验收。

### 15 分类管理和用户管理

- **分类**：进入“分类” -> 新建/编辑/删除分类，观察树刷新。
- **用户**：管理员进入用户管理 -> 点击“新建用户” -> 填用户名、密码、邮箱、状态 -> 保存；再点击编辑或删除。
- **预期现象**：用户列表刷新，启用/禁用状态显示变化。
- **当前状态**：可验收，需管理员权限。

### 16 导入文档和草稿箱

- **导入顺序**：进入“文档” -> “导入” -> 拖入 PDF、Word、Excel、PowerPoint、TXT 或 Markdown -> 等待进度完成 -> 点击结果旁“编辑”。
- **草稿顺序**：进入“草稿” -> 搜索草稿 -> 点击编辑 -> 选择“提交审核” -> 保存。
- **预期现象**：导入文件生成草稿；编辑页能看到解析内容；提交后从草稿列表移出并进入审核。
- **当前状态**：可验收，具体格式是否成功取决于解析依赖和文件内容。

### 17 收藏和最近访问

- **收藏**：文档详情 -> 点击“收藏” -> 进入“收藏” -> 点击刷新/搜索 -> 点击文档标题查看 -> 点击删除取消收藏。
- **最近访问**：打开几篇文档 -> 进入最近访问 -> 点击记录打开文档 -> 点击单条删除或“清空”。
- **预期现象**：收藏和访问记录持久化，刷新后仍存在。
- **当前状态**：可验收。

### 18 PDF 下载和文档分享

- **PDF**：打开文档详情 -> 点击“下载 PDF” -> 浏览器下载文件 -> 用 PDF 阅读器检查中文、标题、代码块、表格和图片。
- **分享**：详情 -> 点击“分享” -> 选择公开/私有、过期时间、访问次数和密码 -> 点击“创建分享链接” -> 点击“复制链接” -> 用未登录窗口打开 `/share/{shareId}`。
- **预期现象**：PDF 可下载；分享页可按权限访问；详情中可删除分享链接。
- **当前状态**：可验收，需中文字体和文件服务配置正确。

## 19-25 AI、检索、图谱、媒体和团队

### 19 AI 助手

- **操作顺序**：进入“智能助手” -> 点击“新对话” -> 在模型下拉框确认 `gpt-5.5` -> 输入“只回复：AI验收成功” -> 点击“发送”。
- **预期现象**：回复区域逐段出现内容；回复支持 Markdown；回复下方可以点👍/👎；左侧能切换和删除会话。
- **当前状态**：AI 对话已部署并验证通过；模型是 OpenAI 兼容配置，不是 Qwen 服务。

### 20 RAG 知识库

- **预期操作**：进入智能助手 -> 打开“知识库”开关 -> 提问与已上传文档有关的问题 -> 查看回答下方引用来源。
- **预期现象**：回答带文档引用和片段；切换开关改变检索路径。
- **当前状态**：暂不可验收。服务器当前 `RAG_ENABLED=false`，embedding 接口未验证，因此知识库开关不能代表 RAG 已可用。

### 21 Neo4j 入门

- **功能点**：节点、关系、路径、Cypher 和 Spring Data Neo4j 基础。
- **前端操作**：无。进入 Neo4j Browser 执行创建、查询、更新、删除 Cypher。
- **当前状态**：无前端操作。

### 22 知识图谱

- **操作顺序**：启动 Neo4j 和图谱服务 -> 登录前端 -> 进入“知识图谱” -> 点击刷新 -> 输入实体关键词 -> 点击“搜索”。
- **预期现象**：显示节点/关系数量；画布出现可拖拽、可缩放的图；搜索后图谱更新。
- **当前状态**：基础图谱页面可验收，服务器已启动 Neo4j 和 `kb-graph`；实体抽取和文档自动建图仍需准备文档数据后验证。

### 23 智能搜索

- **操作顺序**：启动 Elasticsearch 和搜索服务 -> 进入“检索” -> 输入关键词 -> 选择“关键词”或“智能检索” -> 点击“开始检索”。
- **预期现象**：显示结果卡片、分类、作者、匹配分数；右侧搜索历史可再次点击查询。
- **当前状态**：关键词搜索可验收，服务器已启动 `kb-search` 和 Elasticsearch；智能检索仍依赖 embedding/RAG 配置。

### 24 文件管理、音频和视频

- **操作顺序**：上传媒体文件 -> 进入“文件” -> 按文件类型筛选 -> 点击播放预览 -> 对音视频点击转码按钮 -> 刷新查看转码状态。
- **预期现象**：音频可播放；视频可播放；转码状态从处理中变为完成，完成后使用 HLS 播放。
- **当前状态**：文件列表/基础预览可部分验收；音视频 HLS 转码需要转码消费者、FFmpeg 和消息链路，当前部署未完成完整验证。

### 25 团队空间

- **操作顺序**：进入“团队” -> 点击“新建团队” -> 填团队名称、编码、负责人 ID、描述 -> 保存 -> 点击团队名称 -> 点击“添加成员” -> 输入用户 ID -> 确认。
- **预期现象**：团队列表显示成员数和文档数；选中团队后右侧出现成员列表。
- **当前状态**：可验收，需用户服务正常并准备有效用户 ID。

## 26-33 内容运营与管理

### 26 评价、点赞和收藏完善

- **操作顺序**：打开已发布文档 -> 点击文档点赞/收藏 -> 添加评论 -> 点击评论点赞 -> 到收藏页确认记录。
- **预期现象**：文档和评论计数、个人状态、收藏列表同步更新。
- **当前状态**：可验收；若计数不变，优先检查数据库迁移和文档服务日志。

### 27 导出数据页面

- **操作顺序**：文档列表 -> 点击“导出数据” -> 搜索/筛选 -> 勾选一篇或多篇文档 -> 选择 PDF 或 Markdown -> 点击“导出选中”。
- **预期现象**：下载 ZIP；PDF 模式包含 PDF 文件，Markdown 模式包含 Markdown 文件。
- **当前状态**：可验收，需文档服务和字体资源正常。

### 28 个人中心

- **操作顺序**：右上角账户菜单 -> “个人资料” -> 修改姓名、邮箱、电话、部门或简介 -> 点击“保存资料”。
- **头像**：点击头像区域 -> 选择图片 -> 等待上传成功。
- **密码**：点击“修改密码” -> 输入旧密码、新密码、确认密码 -> 点击确认。
- **预期现象**：资料和头像更新；统计卡片显示文档数、获赞、阅读和评论数；密码修改成功后可重新登录。
- **当前状态**：可验收，统计接口和文件服务需正常。

### 29 统计中心

- **操作顺序**：管理员进入“统计中心” -> 选择周/月/年或自定义日期 -> 切换新增/浏览趋势 -> 切换热门文档和活跃用户排行维度。
- **管理按钮**：点击“刷新”重新加载；“清除缓存”后重新加载；点击文档聚合或用户聚合触发手动聚合。
- **导出**：点击趋势、热门文档或活跃用户区域的“导出”。
- **预期现象**：指标、趋势图、分类分布和排行更新；浏览器下载 CSV。
- **当前状态**：统计服务已运行，页面可进入；具体数据依赖统计表、MQ 事件和聚合任务。

### 30 AI 写作

- **操作顺序**：进入“AI写作” -> 输入主题 -> 输入要求或已有内容 -> 选择内容类型、风格、语气和字数 -> 点击“生成内容”。
- **扩展操作**：生成后点击“扩写”“优化”或“续写”；点击“复制”；点击“插入到文档”。
- **预期现象**：结果区流式或普通显示 Markdown；插入到文档会打开预填充的文档编辑页。
- **当前状态**：AI 服务已部署，可验收；需确认相应写作接口和模型额度。

### 31 文档 AI 智能摘要

- **操作顺序**：打开文档详情 -> 点击“AI生成摘要” -> 等待逐段生成 -> 点击“复制”或查看保存后的摘要。
- **中止/重试**：生成中点击“停止摘要”；已有摘要时按钮变为“重新生成摘要”。
- **预期现象**：摘要流式出现，并自动保存到文档摘要字段。
- **当前状态**：AI 服务已部署，可验收；空文档不能生成摘要。

### 32 文档审核完善

- **操作顺序**：作者提交审核 -> 管理员进入“审核” -> 切换待审核 -> 预览文档 -> 输入意见 -> 通过或驳回。
- **工作区**：也可从审核任务进入审核工作区，在右侧执行通过/驳回，在下方查看审核历史。
- **预期现象**：审核轮次、状态和意见更新；审核结果生成通知。
- **当前状态**：可验收，需管理员账号和审核任务。

### 33 通知中心

- **操作顺序**：点击顶部铃铛 -> 进入通知中心 -> 切换全部/未读/已读 -> 按通知类型筛选 -> 点击通知标题查看详情。
- **管理操作**：点击“标记全部已读”、单条“标记已读”、单条删除或“清空全部”；有链接时点击“打开”。
- **预期现象**：顶部未读数字同步减少；通知详情抽屉打开；链接跳转到文档或审核页面。
- **当前状态**：页面和基础接口可验收；实时通知另见 34。

## 34-41 实时、权限、配置、解析、注册和 AI 记忆

### 34 审核结果 WebSocket 推送

- **操作顺序**：作者浏览器保持登录并打开通知中心 -> 管理员在另一浏览器提交审核结果 -> 作者等待通知。
- **预期现象**：不刷新页面即可出现新通知，铃铛未读数增加；点击通知可打开对应文档/审核页。
- **当前状态**：代码包含 SockJS/STOMP 客户端和后端事件链路，但必须确认网关 WebSocket 路由、RabbitMQ 队列和 `websocket.enabled` 配置，当前未完成独立端到端验收。

### 35 权限管理

- **操作顺序**：管理员进入“权限管理” -> 点击“新建权限” -> 填名称、编码、类型和菜单/API 信息 -> 保存。
- **树操作**：点击权限行的“添加子权限”“编辑”或“删除”；展开树确认层级。
- **权限验证**：给角色分配权限后，用普通用户登录并访问相应页面/接口，确认无权限时跳回工作台或被拒绝。
- **当前状态**：管理员页面和接口存在，可验收；前端路由中的管理权限检查仍应使用真实角色/权限数据验证。

### 36 系统设置

- **操作顺序**：管理员进入“系统设置” -> 依次打开基础、安全、存储、通知、AI、系统状态标签页。
- **保存顺序**：修改开关或输入框 -> 点击当前标签页的“保存” -> 刷新页面确认配置仍在。
- **运维操作**：系统状态页点击“清除缓存”或“创建备份”。
- **预期现象**：基础设置影响前端开关；存储设置影响上传限制；AI 配置显示运行参数；状态页显示数据库、文档数和存储使用量。
- **当前状态**：页面和接口可验收；外部 SMTP、embedding、备份存储等功能需额外配置才能完整生效。

### 37 Word、PDF 等文件解析

- **操作顺序**：进入“文档” -> “导入” -> 分别上传 PDF、DOCX、XLSX、PPTX、TXT、MD -> 等待解析 -> 点击“编辑”。
- **预期现象**：文档草稿内容出现；表格、标题和段落尽量保留；Markdown 图片被转为文件 URL。
- **当前状态**：导入页面支持这些格式；复杂 PDF 表格、图片和特殊字体需要用真实样本逐项验收。

### 38 注册、邮件验证和找回密码

- **注册顺序**：退出登录 -> 打开注册入口 -> 填用户名、邮箱和密码 -> 提交 -> 打开邮件中的激活链接 -> 返回登录。
- **找回顺序**：登录页点击“忘记密码” -> 输入邮箱 -> 打开邮件中的重置链接 -> 输入新密码 -> 返回登录验证。
- **当前状态**：页面流程存在，但服务器未配置真实 SMTP 时，邮件发送、激活和找回密码闭环不可用；不能只凭页面提交成功判定完成。

### 39 文档自动保存

- **操作顺序**：新建或编辑文档 -> 输入标题和内容 -> 停止操作等待自动保存状态变化 -> 关闭或返回页面 -> 再次打开编辑页。
- **恢复顺序**：编辑页出现恢复提示时点击“恢复”；也可点击“自动保存历史” -> 选择记录查看/恢复。
- **预期现象**：保存状态从保存中变为已保存；异常退出后能恢复最近草稿；历史记录可查看。
- **当前状态**：前端自动保存和历史页面存在，需用浏览器断开/刷新场景验证后端 MongoDB 历史记录是否完整。

### 40 MQ 按本地环境隔离

- **功能点**：为 RabbitMQ exchange、queue、routing key 增加实例标识，避免不同本地环境互收消息。
- **前端操作**：无。启动两个不同实例，分别提交审核/产生统计事件，检查各自消费者日志和 RabbitMQ 管理台队列名称。
- **预期现象**：实例 A 只消费 A 的消息，实例 B 只消费 B 的消息。
- **当前状态**：无前端操作，属于部署和消息链路验收。

### 41 AI 流式输出和记忆

- **流式顺序**：进入“智能助手” -> 新建会话 -> 发送问题 -> 观察回答是否逐段出现 -> 再发送“我刚才问了什么？”。
- **记忆顺序**：在同一会话连续发送上下文相关问题；切换到另一会话；再切回原会话。
- **预期现象**：回答不是一次性整体出现；同一会话能利用历史消息；切换会话后历史消息仍存在；删除会话后列表移除。
- **当前状态**：流式 AI 对话已部署并验证；对话历史持久化代码存在。RAG 记忆路径受 `RAG_ENABLED=false` 影响，知识库引用仍不可验收。

## 当前最需要补齐的功能

1. 验证 embedding 接口并开启 RAG，恢复 20 和 41 中的知识库问答与引用。
4. 完成音视频转码消费者、FFmpeg、HLS 和消息链路，补齐 24。
5. 配置 SMTP，端到端验证 38 的注册激活和找回密码。
6. 用两个浏览器完成 34 的审核结果 WebSocket 实时推送验收。
7. 用真实 PDF、Word、Excel、PowerPoint 和含表格/图片的文件完成 37 的样本测试。
8. 在文档编辑页补充标签选择和文档标签关联操作。

## 结论

当前项目不是“所有教程功能都已可用”。已经具备并可优先验收的是登录、文档、分类、评论/点赞/收藏、审核、文件基础上传、导入草稿、PDF/分享、团队、个人中心、统计、AI 对话、AI 写作和 AI 摘要。搜索、图谱、RAG、音视频转码、邮件闭环和部分实时能力仍有明确依赖或未完成端到端验证。
