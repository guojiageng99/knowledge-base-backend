<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.knowledge.base.userauth.mapper.TeamMemberMapper">
    <select id="selectByTeamId" resultType="com.knowledge.base.userauth.entity.TeamMember">
        SELECT id, team_id, user_id, member_role, join_time, create_by FROM kb_team_member WHERE team_id = #{teamId} ORDER BY join_time
    </select>
    <select id="selectByUserId" resultType="com.knowledge.base.userauth.entity.TeamMember">
        SELECT id, team_id, user_id, member_role, join_time, create_by FROM kb_team_member WHERE user_id = #{userId} ORDER BY join_time DESC
    </select>
    <delete id="deleteByTeamIdAndUserIds">
        DELETE FROM kb_team_member WHERE team_id = #{teamId} AND user_id IN
        <foreach collection="userIds" item="userId" open="(" separator="," close=")">#{userId}</foreach>
    </delete>
    <select id="countByTeamId" resultType="java.lang.Long">SELECT COUNT(*) FROM kb_team_member WHERE team_id = #{teamId}</select>
</mapper>
