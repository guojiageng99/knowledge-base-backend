<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.knowledge.base.userauth.mapper.TeamMapper">
    <resultMap id="BaseResultMap" type="com.knowledge.base.userauth.entity.Team">
        <id column="id" property="id"/>
        <result column="team_name" property="teamName"/>
        <result column="team_code" property="teamCode"/>
        <result column="description" property="description"/>
        <result column="parent_id" property="parentId"/>
        <result column="level" property="level"/>
        <result column="path" property="path"/>
        <result column="member_count" property="memberCount"/>
        <result column="doc_count" property="docCount"/>
        <result column="leader_id" property="leaderId"/>
        <result column="sort" property="sort"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="create_by" property="createBy"/>
        <result column="update_by" property="updateBy"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <sql id="BaseColumns">id, team_name, team_code, description, parent_id, level, path, member_count,
        doc_count, leader_id, sort, status, create_time, update_time, create_by, update_by, deleted</sql>

    <select id="selectByParentId" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/> FROM kb_team WHERE parent_id = #{parentId} AND deleted = 0 ORDER BY sort, id
    </select>
    <select id="selectByTeamCode" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/> FROM kb_team WHERE team_code = #{teamCode} AND deleted = 0 LIMIT 1
    </select>
    <select id="selectByStatus" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/> FROM kb_team WHERE status = #{status} AND deleted = 0 ORDER BY sort, id
    </select>
    <select id="selectByLevel" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/> FROM kb_team WHERE level = #{level} AND deleted = 0 ORDER BY sort, id
    </select>
    <select id="selectRootTeams" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/> FROM kb_team WHERE parent_id = 0 AND deleted = 0 ORDER BY sort, id
    </select>
    <select id="selectByLeaderId" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/> FROM kb_team WHERE leader_id = #{leaderId} AND deleted = 0 ORDER BY sort, id
    </select>
    <select id="selectByPathPrefix" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/> FROM kb_team WHERE path LIKE CONCAT(#{path}, '%') AND deleted = 0 ORDER BY level, sort, id
    </select>
    <select id="selectTeamTree" resultMap="BaseResultMap">
        SELECT <include refid="BaseColumns"/> FROM kb_team WHERE deleted = 0 ORDER BY level, sort, id
    </select>
    <update id="updateMemberCount">UPDATE kb_team SET member_count = #{count} WHERE id = #{teamId} AND deleted = 0</update>
    <update id="incrementMemberCount">UPDATE kb_team SET member_count = member_count + 1 WHERE id = #{teamId} AND deleted = 0</update>
    <update id="decrementMemberCount">UPDATE kb_team SET member_count = GREATEST(member_count - 1, 0) WHERE id = #{teamId} AND deleted = 0</update>
    <update id="updateDocumentCount">UPDATE kb_team SET doc_count = #{count} WHERE id = #{teamId} AND deleted = 0</update>
    <update id="incrementDocumentCount">UPDATE kb_team SET doc_count = doc_count + 1 WHERE id = #{teamId} AND deleted = 0</update>
    <update id="decrementDocumentCount">UPDATE kb_team SET doc_count = GREATEST(doc_count - 1, 0) WHERE id = #{teamId} AND deleted = 0</update>
    <select id="checkTeamCodeExists" resultType="java.lang.Boolean">
        SELECT COUNT(*) &gt; 0 FROM kb_team WHERE team_code = #{teamCode} AND deleted = 0
        <if test="id != null"> AND id != #{id}</if>
    </select>
    <select id="countAll" resultType="java.lang.Long">SELECT COUNT(*) FROM kb_team WHERE deleted = 0</select>
    <select id="countEnabled" resultType="java.lang.Long">SELECT COUNT(*) FROM kb_team WHERE status = 1 AND deleted = 0</select>
</mapper>
